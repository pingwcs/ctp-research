# Created in the persistent runtime directory on first start.
# Secret values are generated by production.ps1 and must never be committed.
APPAPI_HOST=127.0.0.1
APPAPI_PORT=8000
MARKET_DATA_DIR=../data/output
